node_modules/
dist/

# Secrets — keep .env.example committed as a template only
.env
.env.local
*.log
.DS_Store
client/dist/
server/.env

# Real faculty mapping for the catalogue importer (client data; keep the .example)
faculty-map.json
